For each sample, the raw counts were normalized to a total count of 10,000 and then log-transformed.
PCA was performed on the normalized data, and the top 30 principal components were chosen for calculating the k nearest neighbors (kNN).
